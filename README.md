# hauntedmansion-bk

Waitlist teaser for a haunted attraction in Bushwick, Brooklyn. Opens **September 25**.

Single self-contained `index.html` — no build step, no dependencies. Fonts come from
Google Fonts; everything else is inline.

## Structure

| Path | What |
|---|---|
| `index.html` | The whole site |
| `img/hero-wide.jpg`, `img/hero-tall.jpg` | Graded hero, 16:9 and 3:4 |
| `img/fog.png` | Seamless tiling fog, generated by `tools/fog.py` |
| `tools/` | Image pipeline (needs `numpy` + `pillow`, and the raw render, which is not committed) |
| `supabase/001_waitlist.sql` | Waitlist table, RLS, and the count function |

## Backend

Signups go to Supabase (`hm_waitlist`). Anon can insert and nothing else — the list
itself is not readable with the public key. A repeat email returns 409, which the page
treats as success. The signup counter calls a `security definer` count function and
stays hidden below `COUNTER_MIN` signups: the number shown is always the real one.

## Before the real domain goes live

- Remove the `robots: noindex` line in `index.html`
- Point `og:image` back at the production domain
