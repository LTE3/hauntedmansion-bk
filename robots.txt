# Preview only. index.html still carries <meta name="robots" content="noindex">;
# when the real domain goes live, delete that line and this block together.
User-agent: *
Disallow: /

# No Sitemap line. There was one, pointing at a sitemap.xml that has never
# existed on a domain that does not resolve - two dead ends in one
# directive. A one-page site does not need one; if the house ever grows
# past one page, add the file first and the line second.
